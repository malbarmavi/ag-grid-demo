import { DefFocusDirective } from './def-focus.directive';

describe('DefFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new DefFocusDirective();
    expect(directive).toBeTruthy();
  });
});
